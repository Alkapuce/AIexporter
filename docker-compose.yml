services:
  aiexporter-server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    ports:
      - "8787:8787"
    environment:
      AIEXPORTER_HOST: 0.0.0.0
      AIEXPORTER_PORT: 8787
      AIEXPORTER_DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
